#!/bin/bash

if [ $# -ne 1 ]; then
	echo Number of arguments invalid
	exit 1
fi

regex='^[0-9]+$'
if ! [[ $1 =~ $regex ]]; then
	echo Not a number
	exit 1
fi

count=1
while [ $count -le $1 ]; do
	touch "file_$count.txt"
	count=`expr $count + 1`
done
